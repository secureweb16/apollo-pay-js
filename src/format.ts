export const successResponse = (
    message: string,
    data?: {},
    status: number = 200
) => {
    
}

export const errorResponse = (
    message: string,
    data?: {},
    status: number = 400
) => {
    //
}